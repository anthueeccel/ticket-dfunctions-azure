/// <summary>
/// Severity level of a support ticket. Drives the priority multiplier used by
/// the pricing formula.
/// </summary>
public enum TicketPriority
{
    Low,
    Medium,
    High,
    Critical
}