/// <summary>
/// The functional area a support ticket falls into. Drives the base price
/// used by the pricing formula.
/// </summary>
public enum TicketCategory
{
    Hardware,
    Software,
    Network,
    Access,
    Other
}